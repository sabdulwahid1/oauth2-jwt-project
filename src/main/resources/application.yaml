spring:
  application:
    name: OAuth-Client-Service

  datasource:
    url: postgresql://oauth_db_de50_user:Y4sMG9R3lzfRKHnIRjzNAVLNBl2u366d@dpg-d870o3navr4c738nm58g-a/oauth_db_de50
    username: oauth_db_de50_user
    password: Y4sMG9R3lzfRKHnIRjzNAVLNBl2u366d

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${GOOGLE_CLIENT_ID}
            client-secret: ${GOOGLE_CLIENT_SECRET}
            scope:
              - openid
              - profile
              - email
            redirect-uri: "{baseUrl}/login/oauth2/code/{registrationId}"

        provider:
          google:
            authorization-uri: https://accounts.google.com/o/oauth2/v2/auth
            token-uri: https://oauth2.googleapis.com/token
            user-info-uri: https://www.googleapis.com/oauth2/v3/userinfo
            user-name-attribute: sub